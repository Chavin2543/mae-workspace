{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "(pip install -q openpyxl pandas >/dev/null 2>&1 &); echo '{\"systemMessage\":\"Mae workspace ready — type /guide to see what this session can do (พิมพ์ /guide เพื่อดูวิธีใช้งานค่ะ)\"}'",
            "timeout": 15,
            "statusMessage": "Preparing Mae's workspace..."
          }
        ]
      }
    ]
  }
}
