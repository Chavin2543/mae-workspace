---
description: Show what's in the workspace right now — files, open tasks, recent decisions
---

Give Mae a short, plain-language status of the workspace (no jargon, Thai hints
welcome). Gather it with quick read-only commands, then present:

1. **Open tasks** — list `tasks/open/` (or "nothing in progress").
2. **Latest deliverables** — newest files in `output/` with dates.
3. **Source files on hand** — what's in `data/source/` and `data/pdf/`.
4. **Recent decisions** — the 3 newest files in `docs/decisions/`, one line
   each.
5. **Git health** — current branch, clean/dirty, anything unpushed.

Keep it to one short screen. Offer next steps (e.g. resume an open task).
