---
description: Save everything to GitHub so every machine sees the latest work
---

Mae wants the current state of the workspace saved and shared. Do this now,
without asking further questions:

1. If there is finished work not yet logged, append a short entry to
   `WORKLOG.md` (date, what was done, files touched — 2–4 plain lines,
   newest first).
2. Commit everything with a clear message describing the work.
3. Push the current branch, then make sure the commits are on the central
   `main` branch too (standing permission — see the centralized-git section
   in CLAUDE.md). If main has moved, fetch and merge `origin/main` first,
   then push again.
4. If there was nothing to save, just say so.

Reply with one plain sentence — no git jargon, no command output. Example:
"Everything is saved — your other computer will see it too. (บันทึกขึ้น GitHub
เรียบร้อยค่ะ)"
